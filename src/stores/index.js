import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const getters = {};
const actions = {};
const mutations = {};
const state = {};

export default new Vuex.Store({
    state,
    getters,
    mutations,
    actions,
});
